-*- mode: ruby -*-
require "rubygems"
require "rake"


namespace :interval do
  task :purge do
    sh "rm -rf .emacs*"
  end
end

task :update => ["interval:purge"] do
  sh "cp ~/.emacs ./"
  sh "cp -r ~/.emacs.d ./"
end


task :install do

end
